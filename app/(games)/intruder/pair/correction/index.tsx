import Correction from '@/components/Correction';
import { StyleSheet } from 'react-native';

export default function IPCorrectionScreen() {

  return (
    <Correction/>
  );
}

const styles = StyleSheet.create({
});